---
description: 不可逆架构决定前必须先做同类产品调研
alwaysApply: true
---

# 不可逆架构决定前，先看别人怎么做

**触发条件**（命中任一即适用）：决定某份状态归哪一层 / 哪个进程；新增或删除跨进程协议；引入新依赖树；选择存储位置与一致性机制；任何「改起来要重写几百行」的决定。

## 强制动作

1. **先查，再写方案。** 手边同级目录 `/Users/yankeben/workspace/` 有 7 个可参考实现：`vscode`、`cline`、`continue`、`codex`、`opencode`、`pi`、`cc-fork-01`。至少查 3 个，优先查架构最同构的（多前端 + 独立后端进程的选 `codex` / `continue` / `vscode`）。
2. **查证而非回忆。** 必须给出**文件路径 + 符号名或测试名**作为证据，不接受「据我所知业界一般……」。
3. **写进方案。** 调研结论、反例、以及**什么条件下这个决策应当被推翻**，三样都要落进方案文档，不能只在对话里说。
4. **找现成的测试。** 如果别人已经把某条不变量写成了测试，直接引用它 —— 那是最强的证据，也说明这个坑有人踩过。

## 为什么（真实代价）

本仓 2026-07 图片附件一役：决定「composer 草稿放 Rust 后端还是扩展层」时没做调研，选了后端权威存储 + CAS + 跨进程两阶段提交。事后逐个查证：

```
  7/7 参考实现都不在后端做草稿权威存储
  7/7 都没有草稿 CAS
  Codex 的 Rust core 连 draft 概念都没有（它的 ComposerDraft 在 tui/ 进程内存里）
  微软早已把「图片载荷与频繁更新的输入状态分开存」写成了一个测试：
    vscode/…/chat/test/browser/widget/input/chatInputStatePersistence.test.ts
    test('stores image payloads separately from frequently updated input state')
```

代价：约 600 行 Rust 返工、8 条 P0/P1 缺陷（它们全是这一个放置决定的下游）、以及写放大（打一个字搬 132MB）与内存放大（11 张图 528MB）两个性能事故。

**调研成本半天，避免的返工量差一个数量级。** 完整记录见 `tomcat-vscode-ext/docs/architecture/image-attachments/01-placement-decision.md`。

## 配套警觉

方案里出现「**瘦身**」「**缓解**」「**降低**」这类动词时，先回头确认那个被优化的东西**是否本就不该存在**。

同一役的第二个例子：为 SVG 栅格化引入 `resvg`（+44 crate、+2.16MiB 产物、冷编译 +83s），初版整改方案是「给 resvg 瘦身」—— 接受了「栅格化必须在 Rust 做」这个未经检验的前提。而 webview 里本来就有 Chromium，搬过去之后依赖归零，顺带把「怎么在 Rust 里安全解析 SVG」整个问题域一起消掉了。
